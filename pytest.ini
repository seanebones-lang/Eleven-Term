[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --strict-markers
    --tb=short
markers =
    security: Security-related tests
    integration: Integration tests
    unit: Unit tests
    slow: Slow tests that take time
